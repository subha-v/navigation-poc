import Foundation

struct ServerConfig {
    // Update this with your Mac's IP address when testing on device
    // To find your IP: System Preferences > Network > WiFi
    static let serverURL = "http://10.0.0.206:8080"
}