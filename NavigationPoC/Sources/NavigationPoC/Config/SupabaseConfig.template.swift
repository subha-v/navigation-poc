import Foundation

// INSTRUCTIONS:
// 1. Copy this file and rename it to "SupabaseConfig.swift"
// 2. Replace the placeholder values with your actual Supabase credentials
// 3. Make sure SupabaseConfig.swift is in .gitignore (it should be already)
// 4. Never commit the actual SupabaseConfig.swift file to version control

struct SupabaseConfig {
    static let url = "https://YOUR_PROJECT.supabase.co"
    static let anonKey = "YOUR_ANON_KEY"
}